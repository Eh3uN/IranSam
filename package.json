{
  "name": "iran-sam-vite",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node scripts/check.mjs && npm run check:data && npm run check:contact && npm run check:experience",
    "check:data": "node scripts/check-data.mjs",
    "check:contact": "node scripts/check-contact.mjs",
    "check:experience": "node scripts/check-experience.mjs"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "tailwindcss": "^4.3.3",
    "vite": "^8.2.2"
  },
  "description": "Persian RTL narrative landing page for Iran Sam Football Academy",
  "engines": {
    "node": ">=22.12.0"
  }
}
